; @hailmary/shared is a direct git dependency (see package.json) — npm 12 defaults
; allow-git to "none", so a plain `npm install`/`npm ci` refuses to fetch it.
; "root" permits git deps declared in this project's own package.json while still
; blocking any transitive one a compromised sub-dependency might introduce.
allow-git=root
